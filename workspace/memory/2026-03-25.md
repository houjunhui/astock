# 2026-03-25 日志

## GitHub 推送完成

- 目标仓库：https://github.com/houjunhui/astock
- 推送分支：sprint/default → main
- Token：（已移除，写入本地 .env）
- 完成提交：1513b4f（初始）+ 1d1e6f1（移除67MB parquet）
- .parquet大文件已从git移除并加入.gitignore

## API Key 安全处理

- `.env` → YOUR_QUICKTINY_KEY_HERE（本机保留真实Key）
- `.env.example` → 示例文件
- `quicktiny.py` → Key占位符
- `model_train_v2.py` → Key占位符
- `.gitignore` → 忽略 .env、cache/、data/、*.parquet

## 0325 预判

- 情绪：退潮 30度（4梯队，ladder规模小）
- 候选：雪浪环境(300385) 2板 LOW — 唯一勉强合格
- 观察：华电辽能(600396) 7板 HIGH — 退潮期HIGH关闭，需竞价确认
- 中利集团(002309) 一字板 — 不碰
- 浙江新能(002218) 封成率50% — 排除

## 教训

- GitHub token via HTTPS，必须用 credential.store，不能依赖 GIT_ASKPASS 或 terminal prompt
- push时"fetch first"错误 = 远程有README；用 `pull --allow-unrelated-histories` 合并
- all_klines.parquet 66MB > 50MB GitHub警告，需要 LFS 或从 git 移除

## quicktiny 新接口接入（10:00-10:15）

### 已新增 9 个接口到 astock/quicktiny.py
1. `get_search(query)` — 股票搜索（名称/代码/行业/拼音）
2. `get_hot_sectors(date)` — 20个热点板块+AI分析+成分股明细
3. `get_limit_events(event_type, limit)` — 实时封板/炸板事件流
4. `get_approaching_limit_up(date)` — 逼近涨停监控（盘中有效，盘后空）
5. `get_anomalies(date)` — 异动检测（历史日期返回空，格式待验）
6. `get_briefings(date, btype)` — 每日简报（盘中返回空，收盘后有）
7. `get_capital_flow_v2(...)` — 增强版资金流向
8. `get_correlation(code)` — 个股关联度
9. `get_concept_stocks(ts_code, date)` — 概念板块成分股

### 龙虎榜接口
- `get_dragon_tiger(date, stock_code, stock_name, page, page_size)`
- 字段：stockCode/stockName/buyBranches/sellBranches（各5席位+买卖金额）
- netBuy/totalBuy/totalSell 资金量化数据
- limitUpInfo 含AI分析（与hot-sectors共享）

### 回测数据缓存脚本
- 路径：`scripts/quicktiny_cache.py`
- 缓存目录：`astock/cache/quicktiny/`
- 已完成 15 天数据（20260304-0324）：hot-sectors/dragon-tiger/limit-events/capital-flow 各15天
- anomalies/briefings 历史数据为空（接口特性）
- 总计 60 文件，约 2.8MB

## auto_close.py Bug 修复（10:14）

### 问题
`NameError: name 'closed' is not defined` — 3次执行均失败

### 根因
高风险强制平仓分支（risk_tag in extreme/high + buy_date==昨天）在 `code = pos["code"]` 赋值前就引用了 `code` 和 `name` 变量

### 修复
1. 将 `code = pos["code"]` / `name = pos["name"]` 移入高风险分支开头
2. 补上漏掉的 `timedelta` import（from datetime import datetime, date, timedelta）

### 触发条件
持仓中任意一只 `risk_tag: high/extreme` 且 `buy_date == 昨天`

## 今日市场（0325 10:00）
- 阶段：主升（temp 82.5，涨停47，跌停6，炸板0）
- 竞价结果：000722湖南发展(B级)，603937丽岛新材(B级)
- 广联航空300900：竞价+1.08%平开，MACD空头，MA20下方，晋级率49.9%临界
